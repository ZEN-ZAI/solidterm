#!/usr/bin/env bash
# Phase 0: stub. Future: enforce that files marked `// AI-AUTHORED` have
# at least one matching test covering the contract.
set -euo pipefail
echo "check-ai-authored-tests: skipped (stub — implement in M1)"
exit 0
